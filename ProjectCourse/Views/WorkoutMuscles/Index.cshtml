@model IEnumerable<ProjectCourse.Models.WorkoutMuscle>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Workout Name
        </th>
        <th>
            Muscle Name
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PrimMover)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Workout.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Muscle.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PrimMover)
        </td>        
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WorkoutMuscleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.WorkoutMuscleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WorkoutMuscleID })
        </td>
    </tr>
}

</table>
