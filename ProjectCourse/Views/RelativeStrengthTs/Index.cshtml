@model IEnumerable<ProjectCourse.Models.RelativeStrengthT>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RelativeStrengthValue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RelativeStrengthPoint)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sex)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Workout.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RelativeStrengthValue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RelativeStrengthPoint)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sex)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Workout.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RelativeStrengthID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RelativeStrengthID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RelativeStrengthID })
        </td>
    </tr>
}

</table>
